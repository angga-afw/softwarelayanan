const register = require('./register');
const getUsers = require('./getUsers');
const iduser = require('./getIdUser');
const update = require('./update');

module.exports = {
    register,
    getUsers,
    iduser,
    update,
}
